# Optional API origin override. Leave unset for local development (the React
# dev server proxies /api to port 3001) and for the combined Railway service.
# Do not include /api; the client adds that prefix.
# REACT_APP_API_URL=https://api.example.com

# Google OAuth Client ID
REACT_APP_GOOGLE_CLIENT_ID=your_google_client_id
