[build]
  base = "/"
  command = "npm run build:web"
  publish = "packages/web/build"

# SPA routing - redirect all routes to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
